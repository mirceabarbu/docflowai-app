---
prompt: 123
titlu: P0-04 — cheia privată STS scoasă din DTO-ul organizației · P0-02 — gardă de tenant pe PUT /admin/users/:id
model_suggested: Sonnet 4.6 (Default)
branch: develop
version_bump: 3.9.752 → 3.9.753
migratii: NU
cache_version_bump: NU (zero fișiere din public/ atinse)
v_param_bump: NU
---

# ⚠️⚠️ BRANCH: `develop` — EXCLUSIV ⚠️⚠️
`main` = PRODUCȚIE, administrat MANUAL de Mircea. NU face niciodată checkout/merge/push pe `main`.
Pasul final OBLIGATORIU: `git push origin develop`.

Prima comandă pe orice stație:
```
git fetch origin && git status && git log --oneline --graph --all -6
```
Trebuie să fii pe `develop`, curat, aliniat cu `origin/develop` (v3.9.752, după #122).

---

# CONTEXT

Ultimele două constatări P0 din auditul extern v3.9.746. Sunt bundle-uite într-un singur prompt fiindcă amândouă sunt gărzi pe rute de administrare, fiecare de câteva linii, fără migrații și fără atingerea frontendului — dar rămân **două etape independente**, fiecare cu testele ei.

## Fapte de cod verificate (folosește-le, nu le re-descoperi)

**P0-04** — `server/routes/admin/organizations.mjs:57`: `GET /admin/organizations/:id` construiește `signingCols = ', signing_providers_enabled, signing_providers_config'` și întoarce rândul brut cu `res.json(rows[0])`. `signing_providers_config` conține `privateKeyPem` (cheia privată RSA folosită la semnarea STS Cloud) și eventuale secrete de client.
- ⚠️ Ruta e platform-admin (`actor.role !== 'admin'` → 403), deci NU e expunere neautentificată. Riscul e că un secret de producție ajunge în browser, în devtools, în cache-uri și în orice log de proxy — fără niciun motiv.
- **Consumatorul verificat: NU EXISTĂ.** Frontendul (`public/js/admin/organizations.js:637-644`) citește configul din ruta DEDICATĂ `/admin/organizations/:id/signing`, care întoarce `configSafe` (construit la `organizations.mjs:410-420`, cu `hasPrivateKey` în loc de cheie). `openProviderConfig` (`:715`) citește exclusiv din `_orgConfigSafe`, iar câmpul de cheie e lăsat GOL cu un placeholder „● ● ● Cheie privată salvată". ⇒ scoaterea câmpului din DTO nu rupe nimic.

**P0-02** — `server/routes/admin/users.mjs:520`: `PUT /admin/users/:id` termină cu `UPDATE users SET … WHERE id=$${i}` — **fără `org_id`**. Poarta de rol e doar `isAdminOrOrgAdmin(actor)`, deci un `org_admin` din organizația A poate edita un utilizator din organizația B: email, compartiment și **parolă** (`password_hash`) ⇒ preluare de cont, nu doar editare.
- Rutele SURORI din ACELAȘI fișier au deja garda, cu același cod de eroare `forbidden_cross_tenant`: reset-password (`:580`), delete (`:612`), reactivate (`:686`), send-credentials (`:800`). Toate re-citesc organizația actorului din DB (`SELECT org_id FROM users WHERE id=$1` cu `actor.userId`), NU se bazează pe `actor.orgId` din JWT. PUT-ul e singura excepție ⇒ e o scăpare, nu o decizie.
- Semantica surorilor: garda se aplică DOAR pentru `org_admin`; `admin` (platform) rămâne cross-org. Păstreaz-o.

⛔ ZONĂ NO-TOUCH (neatinsă): `server/signing/providers/STSCloudProvider.mjs`, `server/routes/flows/cloud-signing.mjs`, `server/routes/flows/bulk-signing.mjs`, `server/signing/pades.mjs`, `server/signing/java-pades-client.mjs`.

===============================================================================
# ETAPA A — P0-04: cheia STS nu mai pleacă spre browser
===============================================================================

## Pas A1 — scoate configul brut din DTO (`server/routes/admin/organizations.mjs`, ~57)

Găsește:
```js
    const signingCols = hasSigning ? ', signing_providers_enabled, signing_providers_config' : '';
```
Înlocuiește cu:
```js
    // P0-04 (audit v3.9.746): `signing_providers_config` conține `privateKeyPem` (cheia
    // privată STS) și NU are voie să părăsească serverul. Varianta sigură există deja pe
    // ruta dedicată GET /admin/organizations/:id/signing (`configSafe`, cu `hasPrivateKey`
    // în loc de cheie) — singura pe care o citește frontendul. Aici expunem DOAR lista de
    // provideri activi. ⛔ Nu readăuga coloana „ca să fie la îndemână".
    const signingCols = hasSigning ? ', signing_providers_enabled' : '';
```

## Pas A2 — inventar: mai pleacă vreun secret dintr-o rută?
Rulează și raportează rezultatul EXACT (nu repara nimic în afara Pasului A1 fără să întrebi — dacă găsești altă rută care întoarce `signing_providers_config` brut către client, OPREȘTE-TE și raportează, e o constatare nouă, nu scope-ul acestui prompt):
```
grep -rn "signing_providers_config" server/routes/ --include=*.mjs
# Așteptat după A1: DOAR locuri care CITESC configul pe server (PUT-uri, /signing cu configSafe,
# /admin/signing/verify) — niciun `res.json` care să întoarcă obiectul brut.
```

## Pas A3 — PUT să nu mai poată ȘTERGE un secret stocat (hardening, `~185`)

`PUT /admin/organizations/:id` face azi `params.push(JSON.stringify(signing_providers_config))` — înlocuire TOTALĂ a JSONB-ului. Un apelant care trimite un config parțial (fără `privateKeyPem`, exact ce face un client care nu mai primește cheia) șterge tăcut cheia din DB și rupe semnarea STS.

Merge-ul corect EXISTĂ deja în acest fișier, la `PUT /admin/organizations/:id/signing` (~462): citește `existingConfig`, apoi `mergedConfig[pid] = { ...(existingConfig[pid] || {}), ...cfg }`. **Oglindește-l fidel** în PUT-ul general, în locul înlocuirii totale. ⛔ Nu inventa altă strategie de merge; nu schimba ruta `/signing`.

### Verificare A
```
grep -n "signing_providers_config" server/routes/admin/organizations.mjs
# Așteptat: NICIO apariție în SELECT-ul lui GET /admin/organizations/:id
grep -n "mergedConfig" server/routes/admin/organizations.mjs
# Așteptat: două locuri (ruta /signing existentă + PUT-ul general, după A3)
```

## Pas A4 — teste (`server/tests/` — extinde fișierul existent pentru admin/organizations dacă există, altfel creează `server/tests/db/admin-org-secret-leak.test.mjs`)
Importă din PRODUCȚIE (montează app-ul real), NU redeclara logica.
1. Seed: o organizație cu `signing_providers_config = {"sts-cloud":{"clientId":"c1","kid":"k1","privateKeyPem":"-----BEGIN RSA PRIVATE KEY-----TEST-----END RSA PRIVATE KEY-----"}}`.
2. `GET /admin/organizations/:id` ca platform-admin → 200, iar în răspuns:
   - `signing_providers_config` ABSENT;
   - `signing_providers_enabled` PREZENT (nu am rupt UI-ul);
   - **aserțiunea tare**: `JSON.stringify(res.body)` NU conține `privateKeyPem` și nici `BEGIN RSA PRIVATE KEY` — scanare pe tot corpul, nu doar pe cheia de nivel 1. Asta prinde și o eventuală re-expunere prin alt câmp.
3. Control POZITIV: `GET /admin/organizations/:id/signing` → `configSafe['sts-cloud'].hasPrivateKey === true` ȘI `configSafe['sts-cloud'].privateKeyPem` undefined. (Calea pe care o folosește chiar UI-ul continuă să funcționeze.)
4. A3: `PUT /admin/organizations/:id` cu `signing_providers_enabled: ['local-upload','sts-cloud']` și `signing_providers_config: {"sts-cloud":{"clientId":"c2"}}` (FĂRĂ cheie) → după update, `privateKeyPem` din DB e NESCHIMBAT, iar `clientId` e `c2`.

===============================================================================
# ETAPA B — P0-02: gardă de tenant pe PUT /admin/users/:id
===============================================================================

## Pas B1 — garda (`server/routes/admin/users.mjs`, în ruta de la ~520)

Inserează garda în blocul `try`, ÎNAINTE de `UPDATE`-ul de utilizator (deci și înaintea oricărei scrieri), oglindind forma din rutele surori din același fișier:

```js
    // P0-02 (audit v3.9.746): fără această gardă, UPDATE-ul rula cu `WHERE id=$n` — un
    // org_admin din organizația A putea schimba parola unui utilizator din organizația B
    // (preluare de cont). Aceeași formă ca la reset-password/delete/reactivate/
    // send-credentials din acest fișier. Platform-adminul (`role === 'admin'`) rămâne
    // cross-org, ca la surori.
    const { rows: targetRows } = await pool.query(
      'SELECT org_id FROM users WHERE id=$1', [targetId]);
    if (!targetRows.length) return res.status(404).json({ error: 'user_not_found' });
    if (actor.role === 'org_admin') {
      const { rows: actorRows } = await pool.query(
        'SELECT org_id FROM users WHERE id=$1', [actor.userId]);
      const actorOrgId = actorRows[0]?.org_id || null;
      // FAIL-CLOSED: dacă nu putem DOVEDI că e același tenant, refuzăm. (Surorile sunt
      // permisive când un org_id e NULL; aici alegem strict — un org_admin fără org_id
      // e o stare invalidă, nu o permisiune.)
      if (!actorOrgId || actorOrgId !== targetRows[0].org_id) {
        logger.warn({ actorId: actor.userId, targetId, actorOrgId, targetOrgId: targetRows[0].org_id },
          '[SEC] PUT /admin/users/:id cross-tenant REFUZAT');
        return res.status(403).json({ error: 'forbidden_cross_tenant' });
      }
    }
```
Notă de implementare: interogarea de rol existentă pentru bump-ul `token_version` (SEC-87) rulează ÎNAINTE de `try` — o lași exact unde e. Garda de mai sus e prima instrucțiune din `try`. ⛔ Nu muta și nu rescrie logica SEC-87.

⛔ Nu schimba `WHERE id=$${i}` din UPDATE (garda de mai sus e suficientă și e verificabilă în teste). Dacă alegi totuși să adaugi și scoparea în UPDATE, e o abatere care trebuie raportată explicit.

## Pas B2 — teste (`server/tests/db/admin-users-tenant-guard.test.mjs`, NOU)
Două organizații — ⚠️ `seedOrgUser` are `orgName='Org Test'` implicit; două org-uri cu numele implicit calcă `organizations_name_key`, deci dă nume distincte (pattern-ul există în `alop-tranzitii-garzi.test.mjs:97`).
1. NEGATIV: `org_admin` din org A face `PUT /admin/users/:id` pe un user din org B, cu `{ password: 'parolanoua' }` → **403 `forbidden_cross_tenant`**, iar în DB `password_hash` și `email` ale țintei sunt NESCHIMBATE (verifică DB, nu doar codul de status — asta e diferența dintre „a refuzat" și „a refuzat DUPĂ ce a scris").
2. POZITIV: `org_admin` din org A pe un user din org A → 200, câmpul se modifică.
3. POZITIV: platform-`admin` pe un user din ALTĂ org → 200 (comportament păstrat deliberat, ca la rutele surori).
4. NEGATIV: țintă inexistentă → 404 `user_not_found`.
5. Paritate: același caz cross-tenant pe `POST /admin/users/:id/reset-password` → 403 (control că nu am rupt garda surorilor).

## Pas B3 — restul suitei
Rulează toată suita și repară fixturile oricărui test care edita utilizatori cross-org fără să vrea. ⛔ Dacă un test devine roșu, întrebarea e „premisa testului era greșită?", nu „cum slăbesc garda?". Un test roșu care NU se explică prin premisă greșită ⇒ OPREȘTE-TE și raportează.

===============================================================================
# ETAPA C — versionare, rulare, push
===============================================================================

## Pas C1 — `package.json`: `3.9.752` → `3.9.753`.
⛔ FĂRĂ bump `CACHE_VERSION`, FĂRĂ `?v=` — acest prompt nu atinge niciun fișier din `public/`.

## Pas C2 — rulare completă (poartă înainte de push)
```
npm test
npm run test:db      # rețeta PG 17 efemeră din CLAUDE.md
```
Ambele VERZI. `test:db` PASSED REAL, nu „skipped" — ambele gărzi sunt pure DB; un mock pe `pool.query` ar testa forma, nu comportamentul.

## Pas C3 — commit + push
```
git add -A
git commit -m "fix(#123): P0-04 cheia STS scoasă din DTO organizație + merge config la PUT · P0-02 gardă de tenant pe PUT /admin/users/:id — v3.9.753"
git push origin develop
```

===============================================================================
# RAPORT FINAL (completează după rulare)
===============================================================================
- Commit hash (develop): __________ · push: __________
- `npm test`: ____ fișiere / ____ teste (verde? da/nu)
- `npm run test:db`: ____ fișiere / ____ teste — PASSED REAL? (da/nu)
- A1: `signing_providers_config` mai apare în vreun `res.json` către client? __________
- A2: ieșirea EXACTĂ a grep-ului (listă rute) — vreo constatare nouă? __________
- A3: merge-ul din PUT oglindește ruta `/signing`; testul 4 (cheia supraviețuiește unui PUT parțial) e VERDE? __________
- B1: garda respinge ÎNAINTE de orice scriere — testul 1 confirmă `password_hash` neschimbat în DB? __________
- B: platform-adminul a rămas cross-org (testul 3 verde)? __________
- Teste migrate/reparate la B3 — listă + premisa greșită în fiecare: __________
- Abateri de la prompt + motiv: __________

===============================================================================
# ⛔ CONSTRÂNGERI ABSOLUTE
===============================================================================
- ⛔ EXCLUSIV `develop`. Pasul final = `git push origin develop`.
- ⛔ Zona NO-TOUCH de semnare neatinsă. FĂRĂ migrații. FĂRĂ atingerea vreunui fișier din `public/`.
- ⛔ NU atinge ruta `/admin/organizations/:id/signing` (`configSafe`) — e calea sigură, deja corectă, și singura pe care o folosește UI-ul.
- ⛔ NU schimba semantica surorilor: platform-`admin` rămâne cross-org; garda e DOAR pentru `org_admin`.
- ⛔ Fail-CLOSED: orice ramură în care nu se poate DOVEDI același tenant = refuz. Nicio eroare înghițită tăcut.
- ⛔ NU slăbi o gardă ca să treacă un test. Premisă greșită sau raport.
- ⛔ Citește fiecare fișier ÎNAINTE de patch; `old_str` unic (whitespace inclus).
